## T08：端到端联调与人工回归

### 目标

在前序修复完成后，统一收口并关闭 RT01–RT08。  
这是最后的 **真实 GUI / MCP / Runtime 联调与人工回归** 任务。

本任务的重点不是再大改架构，而是：

1. 用真实环境重新跑一遍问题矩阵
2. 证明前序修复已经在真实链路上生效
3. 把最终测试流程沉淀为可复用文档或脚本

---

### 依赖

建议在 `T01`–`T07` 全部完成后执行。

---

### 建议先读

- `LocalDocs/RuntimeTest/00-主文档.md`
- `LocalDocs/RuntimeTest/01-应用与运行时问题.md`
- `LocalDocs/RuntimeTest/02-任务与协议问题.md`
- `LocalDocs/RuntimeTest/03-安装与IPA兼容性问题.md`
- `99-经验教训.md`

---

### 推荐验证矩阵

#### 1. 只读 / 诊断类

- `list_installed_apps`
- `get_app_info`
- `get_app_settings`
- `preview_entitlements`
- `validate_app_signing`
- `list_keymaps`
- `get_keymap`

#### 2. 可逆写操作

- `update_app_settings`
- `create_keymap`
- `rename_keymap`
- `reset_keymap`
- `delete_keymap`

#### 3. task / 协议类

- 标准 task tools 的查询 / 列表 / 取消
- SSE task status 通知
- 长任务失败原因是否能直接通过标准路径拿到

#### 4. runtime / 输入类

- `launch_app`
- `create_session`
- `list_sessions`
- `tap`
- `long_press`
- `swipe`
- `drag`
- `press_key`
- `type_text`
- `toggle_debug_overlay`
- `close_session`

#### 5. 安装链路类

- 对已知无 ARM64 的 IPA 做**负例验证**，确认能更早、更清晰失败
- 如有可用 ARM64 IPA，可再补正例安装 / 卸载回归

---

### 强约束

- **绝对不要卸载 `原神`**
- `QQ飞车` 的卸载 / 安装只能在明确需要时做，并在文档里记录
- 对给定 IPA 的验证优先作为**负例**使用，除非其兼容性已被前序任务重新确认

---

### 推荐工作内容

1. 把最终人工回归流程沉淀成：
   - 文档步骤，或
   - 新增脚本（如果确实合适）
2. 将每个 RT 问题标记为：
   - 已修复
   - 部分修复
   - 非 bug，已改进诊断
   - 仍阻塞
3. 若仍有残留问题，必须明确：
   - 复现条件
   - 当前证据
   - 是否阻塞上线 / 后续 MCP 使用

---

### 验收标准

完成后应满足：

1. `LocalDocs/RuntimeTest` 中的 RT01–RT08 都有最终结论
2. 新流程可被下一位开发者复用，而不是只存在聊天记录里
3. 全量自动化回归通过
4. 真实 GUI / MCP / Runtime 链路至少完成一轮可信验证

---

### 建议测试

先跑自动化全量回归：

```bash
cd /Users/songdogwang/Codes/PlayCover && xcodebuild test -scheme PlayCoverMCP -destination 'platform=macOS,arch=arm64' -derivedDataPath /tmp/PlayCover-DerivedData FASTLANE=1 CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=YES
```

```bash
cd /Users/songdogwang/Codes/PlayCover && WITH_REGRESSION=1 DERIVED_DATA_PATH=/tmp/PlayCover-DerivedData /bin/bash /Users/songdogwang/Codes/PlayCover/Scripts/test_http_mcp.sh
```

然后做人工 / 真实环境回归，并把新增流程写回文档或脚本。

---

### 2026-03-30 原神 live 回归结果

#### 已确认

- `list_installed_apps` / `get_app_info` / `get_app_settings` / `check_playtools_installed` 可正常读取真实环境状态
- `preview_entitlements` 在 `com.miHoYo.Yuanshen` 上仍失败，错误为“未能读取数据，因为它的格式不正确”
- `validate_app_signing` 在 `com.miHoYo.Yuanshen` 上仍返回：
  - `signed = true`
  - `infoPlistSigned = true`
  - `entitlementsPresent = false`
- `launch_app` + `create_session(timeout=30)` 在 **fresh launch** 条件下成功，`list_sessions` 可见 `ready` session
- 真实 runtime 输入链路中：
  - `tap`（整型坐标）成功
  - `swipe`（整型坐标）成功
  - `press_key`、`type_text`、`toggle_debug_overlay` 成功
  - `long_press`、`drag` 本轮出现 `Bridge operation timed out: Receive timed out`
- keymap 真实链路中：
  - `rename_keymap` 返回成功后，首次 `list_keymaps` 仍可能回读到旧名
  - `delete_keymap` 返回成功后，首次 `list_keymaps` 仍可能回读到已删项
  - 二次回读后列表恢复正确，且 `get_keymap` 已能证明磁盘状态已变化

#### 本轮结论

1. `RT03` 的主路径修复是有效的，但真实回归必须基于 **fresh launch**；若 host 侧 `PlayCover` 重启而 app 旧进程未重启，runtime 不会自动重新注册，`create_session` 仍会超时
2. `RT01` / `RT02` 在真实原神上仍未关闭
3. `RT04` 在上一轮真实原神上曾观察到“写操作成功但首次列表回读滞后”的一致性问题，但该结论需要继续用最新 live 环境复核
4. 本轮额外发现：触控类 MCP tool 之前对整型坐标参数的解析在 live 环境确有问题；修复 `TouchTools` 后，`tap` / `swipe` 已完成真实回归验证

### 2026-03-30 晚间补充 live 回归结果

#### 新增确认

- 对 `com.miHoYo.Yuanshen` 再次执行 `preview_entitlements`，仍报：`Failed to dump entitlements: 未能读取数据，因为它的格式不正确。`
- 对同一 app 再次执行 `validate_app_signing`，仍返回：
  - `signed = true`
  - `infoPlistSigned = true`
  - `entitlementsPresent = false`
- `launch_app` + `create_session(timeout=30)` 再次成功，拿到新的 `ready` session（`runtimePort=60057`）
- 在该 fresh live session 上，`long_press(x=200,y=200,durationMs=800)` 成功
- 在该 fresh live session 上，`drag(start=240,240 end=320,320 durationMs=500 holdDelayMs=120 steps=10)` 成功
- 对 keymap 又做了两轮真实原神回归：
  - `create_keymap(rt08-live-temp-3)` → `rename_keymap(... -> rt08-live-renamed-3)` 后，首次 `list_keymaps` 已直接返回新名；`get_keymap(旧名)` 直接 not found、`get_keymap(新名)` 可成功读取
  - `delete_keymap(rt08-live-renamed-3)` 后，首次 `list_keymaps` 已不再返回已删项；`get_keymap(已删名)` 直接 not found
  - 使用另一组临时名（`rt08-live-temp-4 -> rt08-live-renamed-4`）重复后结果一致，仍未复现列表滞后

#### 补充结论

1. `RT01` / `RT02` 仍稳定存在，是 T08 当前最明确的剩余诊断问题
2. `RT03` 主路径在 fresh live 条件下保持稳定；`create_session` 不再是当前主要阻塞
3. `long_press` / `drag` 在真实原神 fresh session 中已回归通过，上一轮 timeout 更像是临时环境 / 会话状态问题，而不是当前稳定缺陷
4. `RT04` 目前在 fresh / headless live 条件下已无法稳定复现，当前更像带 GUI keymap 页面 / 缓存状态的条件复现；后续若要继续关闭，需带界面条件补证据

### 2026-03-30 最终 live 回归结果（重装 GUI 后）

#### 新增确认

- 使用 `./BuildScripts/build_and_install.sh` 重建并重装 GUI 后，live HTTP MCP 已命中最新 `PlayCover` 内嵌 server
- 对 `com.miHoYo.Yuanshen` 调用 `preview_entitlements` 成功，返回 `18 key(s)`；`appSandbox = true`，`sandboxProfileRules = 14`
- `validate_app_signing` 返回：
  - `signed = true`
  - `infoPlistSigned = true`
  - `entitlementsPresent = true`
  - `valid = true`
- 对 keymap 做真实原神写后读回归：
  - `create_keymap(rt-live-20260330-2242)` 成功
  - `rename_keymap(... -> rt-live-20260330-2242-renamed)` 后，首次 `list_keymaps` 直接返回新名
  - `get_keymap(旧名)` 直接 `Keymap not found`，`get_keymap(新名)` 成功
  - `delete_keymap(rt-live-20260330-2242-renamed)` 后，首次 `list_keymaps` 已不再返回该项，`get_keymap(已删名)` 直接 `Keymap not found`
- 自动化基线已同步满足：
  - `PlayCoverMCP` 全量测试通过（`678 tests, 1 skipped, 0 failures`）
  - `Scripts/test_http_mcp.sh` 通过

#### 最终结论

1. `RT01` 已关闭：问题根因是 entitlements dump 链路对 `codesign` 混合输出解析不稳；修复后真实原神已稳定返回结构化摘要
2. `RT02` 已关闭：签名诊断语义已与真实结果对齐；真实原神现为 `valid = true` 且 `entitlementsPresent = true`
3. `RT04` 已关闭：在最新代码与重装 GUI 后，真实原神上的 keymap `create → rename → immediate list/get → delete → immediate list/get` 主路径已稳定通过
4. `T08` 验收项已满足：自动化全量回归通过，且真实 GUI / MCP / Runtime 链路已完成可信收口

### 非目标

- 不建议在本任务中继续做大规模代码重构
- 发现新问题时，可以记录并开后续任务，但不要把本任务无限扩张

---

### 完成后必须更新

- `01-任务状态.md`
- `99-经验教训.md`
- `LocalDocs/RuntimeTest` 下的最终问题文档
- 本文档中的“最终回归结果 / 新增测试流程 / 残留问题”
