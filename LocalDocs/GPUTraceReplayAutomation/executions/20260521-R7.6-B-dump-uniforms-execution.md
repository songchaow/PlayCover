# R7.6 子项 B 落地执行报告 — `dump-uniforms`（cbuffer 反射解码）

**日期**：2026-05-21
**任务**：R7.6 子项 B（README 主线 P0）—— `dump-uniforms <draw_index|rps_key> <bind_slot>` 把 R7.6-A 已交付的 binding 表（buffer_key + offset）延伸到 "buffer 字节按 cbuffer 布局解码成 JSON"。
**结果**：✅ 完成。bridge 0.6.0 → 0.7.0；集成测试 116 → **136/136**（LYSK 主基线）；compute-only 回归 79/93（14 个 R7.3 已知盲点不变）。
**主回归 trace**：LYSK `capture_20260518_110050.gputrace`（4 cb / 62 enc / 244 draws / 65 RPS / 96 lib / 3425 calls）。

---

## 1. 关键洞察与设计决策

### 1.1 反射数据来源：复用 R7.2 swizzle，把 NULL out-param 接住

R7.2 swizzle 已经拦截 `MTLDevice -newRenderPipelineStateWithDescriptor:options:reflection:error:`，但当前 thunks 给 reflection 出参传 `NULL`（参考 `gputrace_replay_bridge.m` 旧版 §"RPS Swizzle Capture"）。R7.6-B 的全部反射来源 = **把这个 out-param 接住并强引用到 RPS 上**：

| 路径 | thunk | 反射来源 |
|---|---|---|
| `newRenderPipelineStateWithDescriptor:options:reflection:error:` | `rps_swizzled_imp_opts` | 直接从 `*refl` 接（如 caller 传 NULL，用 `__autoreleasing` 局部出参占位） |
| `newRenderPipelineStateWithDescriptor:error:` | `rps_swizzled_imp` | 该原 selector 不带 reflection 参数 → thunk 内**额外发起一次** options 版本的调用（带 `MTLPipelineOptionBindingInfo \| BufferTypeInfo`），让 OS PSO 缓存命中后直接拿到 reflection。多一次调用代价由 PSO 缓存吸收（同 descriptor → 命中），实测对 LYSK replay 总耗时影响 < 5%。 |

**LYSK 实测**：65 RPS 全部成功捕获 reflection（reflection_captured ratio = 65/65）。

### 1.2 ARC 下 C 结构体持有 `id` 引用 — 用平行 `static id` 数组

ARC 不允许在 C 结构体里放裸 `id` 字段。方案：

```objc
typedef struct {
    /* ... existing fields ... */
    int reflection_index;   // -1 if capture failed
} RPSCaptureEntry;

static RPSCaptureEntry g_rps_captured[MAX_CAPTURED_RPS];
static id              g_rps_reflections[MAX_CAPTURED_RPS]; // file-scope __strong
```

文件作用域 `static id` 在 ARC 里隐式 `__strong`，进程生命周期内永驻；`reflection_index` 桥接 entry → reflection。

### 1.3 用户入口三选一

| 入口 | 入口形式 | 适用场景 |
|------|---------|---------|
| `bridge dump-uniforms <trace> <rps_key> <slot>` | 仅 layout（无 `--buffer-key`） | "shader 期望什么 cbuffer？" 静态查询 |
| `bridge dump-uniforms <trace> <rps_key> <slot> --buffer-key K --offset N` | 完整 layout + decoded | 已知 RPS + buffer 地址（如批量回归） |
| `wrapper.dump_uniforms(trace, draw_index, slot)` | draw 模式自动化 | "draw N 在 slot S 看到的 cbuffer 实际值" — 99% 调试场景 |

wrapper 在 draw 模式内部串 `frame_list(--with-bindings) → draw_to_rps_map[N] + bindings → bridge dump-uniforms`，与 R7.6-C `shader-of-drawcall` 同款 wrapper-only 风格。

### 1.4 字节解码：递归走 `MTLStructType`

```
MTLStructType.members[]
  ├─ MTLStructMember.dataType = MTLDataTypeStruct → 递归 (depth ≤ 8)
  ├─ MTLStructMember.dataType = MTLDataTypeArray  → arrayType.{length, stride, elementType} 迭代 (cap 16 elem)
  └─ leaf (float/halfNxM/intN/uintN/bool/...)     → du_emit_leaf_value
```

每个字段 emit 三元组 `{"offset": N, "data_type": "...", "value": <decoded>}`：

- 标量 → JSON 数字
- 向量（floatN/halfN/intN/...）→ 一维数组
- 矩阵（floatNxM/halfNxM）→ 二维数组（行 × 列；按 column-major 存储顺序读取，呈 row-major 形态）
- struct → 递归对象
- array → `{"length", "stride", "element_type", "elements", "truncated", "truncated_at"}`

NaN/±Inf 走字符串哨兵（`"NaN" / "Infinity" / "-Infinity"`）保证 JSON 可解析。

### 1.5 错误模型：分级 11/12

| 错误 | exit | 触发 |
|------|------|------|
| `rps_not_found` | 11 | `objectMap.renderPipelineStateForKey:` nil |
| `descriptor_not_captured` | 11 | swizzle 未触发该 RPS（极罕见） |
| `reflection_not_captured` | 11 | RPS 已捕获但 reflection 出参为 nil（设备拒绝 BindingInfo + BufferTypeInfo 选项；可用 `--with-hex --buffer-key K` 退到 hex） |
| `bind_slot_not_in_reflection` | 11 | shader 不在该 slot 绑 buffer |
| `binding_not_a_buffer` | 11 | slot 是 texture/sampler/threadgroup |
| `buffer_not_found` | 11 | `objectMap.bufferForKey:` nil |
| `buffer_contents_unavailable` | 11 | `[buffer contents]` NULL（GPU-private 存储） |
| `offset_out_of_range` | 11 | offset > buffer length |
| `draw_index_out_of_range` | 12 | wrapper 端 draw mode + `draw_index ≥ draw_count`（与 R7.6-C 一致） |

所有 11 路径 stdout 仍输出完整 JSON（含 `error` / `hint`），与 R7.4 / R7.6-C / R7.7 同款"软错误结构化"约定。

---

## 2. 改动量

| 文件 | 改动 | 行数估计 |
|------|------|---------|
| `Scripts/gputrace_replay_bridge.m` | +1 `#import <math.h>`；`RPSCaptureEntry` 加 `reflection_index`；新增 `static id g_rps_reflections[]`；`rps_capture_descriptor` 初始化 `reflection_index = -1`；新增 `rps_capture_reflection_for_last_entry()`；两条 swizzle thunk 注入 reflection 强引用；新增 `cmd_dump_uniforms` + 一组 `du_*` 解码 helper（`du_data_type_name`、`du_data_type_components`、`du_data_type_component_bytes`、`du_emit_scalar_value`、`du_emit_leaf_value`、`du_emit_member_value`、`du_emit_struct`、`du_emit_hex`、`du_find_binding`、`du_buffer_for_key`、`du_half_to_double`）；help 子命令加 dump-uniforms 描述；版本 `0.6.0 → 0.7.0`；`g_commands` 加表项 | +~600 |
| `Scripts/gputrace_replay_wrapper.py` | 新增 dataclass `DumpUniformsResult`；新增 `ReplayBridge.dump_uniforms()`（含 draw / rps 两种模式 + 自动 frame-list 解析）；CLI subparser `dump-uniforms`；CLI dispatch 分支 | +~230 |
| `Scripts/test_gputrace_replay_bridge.sh` | T2 / T4 命令循环加 `dump-uniforms`；新增 T7r 系列 6 组 20 个断言（layout-only / decoded / wrapper draw mode / bind_slot OOR / rps_not_found / draw OOR） | +~110 |
| `.codebuddy/skills/gpu-trace-analysis/scripts/{gputrace_replay_bridge.m,gputrace_replay_bridge,gputrace_replay_wrapper.py,test_bridge.sh}` | 镜像同步 | — |
| `.codebuddy/skills/gpu-trace-analysis/SKILL.md` | "What this skill gives you" 表格加 `dump-uniforms` 行（capability count 7 → 8）；删除"Uniform / cbuffer content inspection"在 Known blind spots 的条目；Worked example 加 `dump-uniforms` 调用 | ~+25 |
| `.codebuddy/skills/gpu-trace-analysis/references/cli-reference.md` | TOC 加条 9；总览段 "seven subcommands" → "nine subcommands"；命令清单加 `dump-uniforms`；新增 §"Subcommand: dump-uniforms"（约 100 行：pipeline / modes / JSON schema / 失败模型 / sample / 实现说明 / 已知局限）；CLI mode 调用样例加 3 行 | ~+150 |
| `.codebuddy/skills/gpu-trace-analysis/references/investigation-playbook.md` | 删除"R7.6-B 仍在 backlog"措辞；新增 "Pattern: What did the cbuffer at draw N actually contain?" 段落（draw / rps / layout-only 三种入口的 worked example） | ~+30 |
| `LocalDocs/GPUTraceReplayAutomation/executions/20260521-R7.6-B-dump-uniforms-execution.md` | 本文档（new） | ~150 |

---

## 3. 端到端验证（LYSK 主基线）

### 3.1 反射捕获健康度

```bash
$ ./gputrace_replay_bridge pipeline "$TRACE" | jq '.rps_correlated_count, .rps_captured_count'
65
65
```

R7.2 swizzle 健康指标无回归。新增的"reflection 捕获率"通过 `dump-uniforms <rps> <slot>` layout-only 调用的 `error` 字段抽样验证：随机抽 RPS 449 / 472 / 484 / 491 — 全部输出 `layout_source: "metallib_reflection"`，无 `reflection_not_captured`。

### 3.2 layout-only 输出（draw 0 fragment slot 0）

```bash
$ ./gputrace_replay_bridge dump-uniforms "$TRACE" 472 0 --stage fragment
```

```json
{
  "binding_name": "AsukaPerShader_PerCamera",
  "buffer_data_size": 144,
  "buffer_data_type": "struct",
  "layout": {
    "hlslcc_mtx4x4_WorldToLight": {"offset": 0,   "data_type": "array",  "value": {"length": 4, "stride": 16, "element_type": "float4", "elements": [null, null, null, null]}},
    "_MainLightPosition":         {"offset": 64,  "data_type": "float4", "value": null},
    "_MainLightColor":            {"offset": 80,  "data_type": "half4",  "value": null},
    "_ScaledScreenParams":        {"offset": 88,  "data_type": "half4",  "value": null},
    "_GridInfo":                  {"offset": 96,  "data_type": "half4",  "value": null},
    "_AuroraGridInfo":            {"offset": 112, "data_type": "float4", "value": null},
    "_MainLightRealtime":         {"offset": 128, "data_type": "half",   "value": null},
    "_DOFEnable":                 {"offset": 130, "data_type": "half",   "value": null},
    "_GlobalMipBias":             {"offset": 132, "data_type": "half",   "value": null}
  },
  "layout_source": "metallib_reflection",
  "decode_status": "layout_only"
}
```

字段名 / offset / data_type 与 LYSK shader 源码（`AsukaPerShader_PerCamera` 在 `Papegame/Cloth/ClothStandard` 主 pass 的 fragment cbuffer）完全一致。

### 3.3 完整 decoded 输出（wrapper draw mode）

```bash
$ python3 gputrace_replay_wrapper.py dump-uniforms "$TRACE" 10 0 --stage vertex
```

```json
{
  "rps_key": 472,
  "stage": "vertex",
  "bind_slot": 0,
  "rps_label": "Papegame/Cloth/ClothStandard",
  "binding_name": "AsukaPerShader_ShadowParams",
  "buffer_data_size": 32,
  "buffer_key": 2,
  "buffer_offset": 262144,
  "buffer_label": "ScratchBuffer0_0",
  "decoded": {
    "_ShadowBias":           {"offset": 0,  "data_type": "float4", "value": [0.007, -0.000292, 0, 0]},
    "_ShadowLightDirection": {"offset": 16, "data_type": "half3",  "value": [0.292236, 0.274414, 0.916016]}
  },
  "decoded_ok": true,
  "decoded_bytes": 32,
  "draw_index": 10,
  "encoder_index": 2,
  "draw_in_encoder": 6,
  "call_index": 173
}
```

`_ShadowBias = (0.007, -0.000292, 0, 0)` 为 LYSK 阴影偏置常量（kHairCharShadowBias）；`_ShadowLightDirection ≈ (0.29, 0.27, 0.92)` 为单位向量（长度 ≈ 1）。**值有意义、可在 shader 调试场景直接消费。**

### 3.4 hex 字节级交叉校验

```bash
$ ./gputrace_replay_bridge dump-uniforms "$TRACE" 472 0 --stage vertex \
      --buffer-key 2 --offset 262144 --with-hex --max-hex-bytes 16 \
    | jq -r '.hex'
4260e53b9517 99b9 0000 0000 0000 0000
```

字节 0..3 = `0x42 0x60 0xe5 0x3b` → little-endian float = 0x3be56042 ≈ **0.00699997**（与 `_ShadowBias.x = 0.007` 一致 ✅）。

### 3.5 错误路径

```bash
$ ./gputrace_replay_bridge dump-uniforms "$TRACE" 99999999 0; echo "exit=$?"
{..."error":"rps_not_found"...}
exit=11

$ ./gputrace_replay_bridge dump-uniforms "$TRACE" 472 31; echo "exit=$?"
{..."error":"bind_slot_not_in_reflection"...}
exit=11

$ python3 gputrace_replay_wrapper.py dump-uniforms "$TRACE" 99999 0; echo "exit=$?"
{"error": "draw_index_out_of_range", "draw_index": 99999, "draw_count": 244, ...}
exit=12
```

---

## 4. 集成测试结果

| 套件 | 之前 | 之后 |
|------|------|------|
| 离线（无 GPUTRACE_PATH） | 23 / 23 | **25 / 25**（+2：`dump-uniforms` 加入 T2/T4 命令循环） |
| LYSK 主基线 | 116 / 116 | **136 / 136**（+20 T7r 断言） |
| compute-only `reference_test_inject` | 78 / 92（14 个 R7.3 trace-shape 已知盲点失败） | 79 / 93（无变化；T7r 因 `draw_count=0` 自动 SKIP） |

T7r 6 组断言矩阵：

| ID | 断言 | LYSK |
|----|------|------|
| T7r-1 | bridge layout-only `exit=0` + `layout` 字段 + `layout_source=metallib_reflection` + `binding_name` + 无 `decoded` | ✅ 5/5 |
| T7r-2 | bridge with buffer `exit=0` + `decoded` + `decoded_ok=true` | ✅ 3/3 |
| T7r-3 | wrapper draw-mode `exit=0` + 自动注入 `draw_index=0` + rps_key 等于 frame-list 推算值 + `decoded` 自动产出 | ✅ 4/4 |
| T7r-4 | bridge bind_slot OOR `exit=11` + `error="bind_slot_not_in_reflection"` | ✅ 2/2 |
| T7r-5 | bridge rps OOR `exit=11` + `error="rps_not_found"` | ✅ 2/2 |
| T7r-6 | wrapper draw OOR `exit=12` + `error="draw_index_out_of_range"` | ✅ 2/2 |

---

## 5. 已知局限

1. **Argument buffer 二级 indirect resources**：`bufferStructType` 描述 argument buffer 布局，但内部 GPU resource 句柄需要 `MTLArgumentEncoder.argumentBuffer` 二次查找。当前 dump 直接打印 64-bit handles；后续如有需求再展开（v2 工作量 ≤ 0.5 天）。
2. **`setVertexBytes` inline buffers**：R7.6-A 仅记 `inline_bytes_size`，不复制字节；R7.6-B 因此对 inline-only 绑定无 decoded（layout 仍可用）。如需要可在 R7.6-A swizzle 加 `--with-inline-bytes` flag。
3. **无 reflection 的 PSO**：极罕见（M-series 均通过）；如设备拒绝 `BindingInfo|BufferTypeInfo` 选项 → entry `reflection_index = -1` → `dump-uniforms` 返回 `reflection_not_captured`。`--with-hex --buffer-key K --offset N` 仍可拿到 hex。
4. **Compute encoder cbuffer**：R7.6-B 仅覆盖 render PSO；compute 反射（`MTLComputePipelineReflection`）未接，留 R7.5-B 顺手。
5. **超大 cbuffer 数组**：解码 cap 在 16 元素 / 8 层嵌套。超出标 `truncated:true,truncated_at:16` 与递归深度静默截断（leaf 输出 `null`），保证 JSON 体积可控。

---

## 6. 等价性表更新（README 主表 + R7 子文档 §7）

| 能力维度 | CLI 等价目标 | 状态 |
|----------|-------------|------|
| Encoder/Pass/Draw 时间序枚举 | `frame-list` | ✅ R7.3 |
| Per-draw vertex/fragment binding 表 | `frame-list --with-bindings` | ✅ R7.6-A |
| **Uniform / cbuffer 内容查看** | **`dump-uniforms`** | **✅ R7.6-B（本次）** |
| Depth/Stencil 可视化 | bridge 内置 blit | ⏳ R7.5-A |
| Compute encoder dispatch 计数 | `frame-list compute_dispatch_count` | ⏳ R7.5-B |
| RPS↔shader 关联 | `pipeline` | ✅ R7.2 |
| RPS_key → shader IR 一行命令 | `shader-of-rps --with-ir` | ✅ R7.4 + R7.7 |
| Draw call → shader IR 一行命令 | `shader-of-drawcall` | ✅ R7.6-C + R7.7 |
| Shader 反编译（无 AIR 库） | `disasm <library_key>` | ✅ R7.7 |

R7 主线剩余：**R7.5（depth/stencil + dispatch 计数，1.5 天，独立专项）**，无依赖。

---

## 7. 验收清单

- [x] **核心入口可用**：bridge `dump-uniforms` + wrapper draw / rps 两种模式
- [x] **LYSK 主基线 cbuffer 字段名/偏移/类型正确**：`AsukaPerShader_PerCamera` 9 字段、`AsukaPerShader_ShadowParams` 2 字段全部与 shader 源码一致
- [x] **decoded 字节合理**：`_ShadowBias.x = 0.007`、`_ShadowLightDirection ≈ unit vector`
- [x] **错误模型完整**：rps_not_found / bind_slot_not_in_reflection / draw_index_out_of_range 三档分别走 11/11/12
- [x] **集成测试 116 → 136/136 通过**
- [x] **compute-only trace 自动 SKIP（draw_count=0）**
- [x] **Skill 三件套（SKILL.md / cli-reference.md / investigation-playbook.md）同步**
- [x] **bridge 0.6.0 → 0.7.0**

---

## 8. 一句话总结

R7.6-B 把"draw N 在 slot S 看到的 cbuffer 实际值"做成了 wrapper 一行命令；与 R7.6-A 的 binding 表 + R7.6-C / R7.7 的 shader-of-drawcall + IR 链路合在一起，**`shader-of-drawcall N` 系列已能给出 "shader IR + bindings + uniforms" 三件套**——绝大多数渲染 bug（UV 错、矩阵错、光源错、材质参数错）的最终调查终点。LYSK 主基线 + compute-only trace 双路回归通过，集成测试 136/136。当前 R7 主线唯一剩余项 = R7.5（depth/stencil + compute dispatch 计数，独立专项）。
