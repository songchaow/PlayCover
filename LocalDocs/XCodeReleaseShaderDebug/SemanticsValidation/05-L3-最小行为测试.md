## L3：最小行为测试

## 目标

在少量高价值样本上建立：

```text
固定输入
  ↓
执行 shader
  ↓
校验输出
```

用来回答：

- 某些结构上“看起来差不多”的样本，行为是否真的一致
- 哪些 lowering 模式需要从“结构风险”升级到“行为风险”

补充定位：

- `L3` 仍然**不是**跨机器日常默认 gate
- `SV-004 / SV-004F` 当前已经退回**后置升级口 + 守护边界**，不再代表当前唯一主线；当前全局主线仍是“全量 corpus 的 L1/L2 测试”
- 默认只有在用户后续明确决定升级到行为层时，才进入这一层
- 若只是维护当前 L1/L2 主线，本文件按需阅读即可，当前日常推进**不必须读取**

## 当前状态（2026-04-09）

- 首版 compute harness 已落地到 `Scripts/ir_semantics_behavior_runner.py` 与 `Scripts/metal_compute_behavior_runner.swift`，最小 offscreen render-second 已落地到 `Scripts/metal_fragment_behavior_runner.swift`
- 入口继续直接复用 `build/semantics-validation/roundtrip/test-data-representatives/gate-summary.json` 的 `layeredDecision.l3Plan.candidateSampleKeys`，默认行为报告直接写回同目录 `behavior-summary.json`
- 当前默认执行样本已稳定收缩为：`test_fast_math_select`、`test_intrinsic_vector_icmp_zext`；当前默认结果为 `pass/pass`
- `Scripts/ir_semantics_behavior_runner.py` 在写完 `behavior-summary.json` 后会自动刷新同目录 `gate-summary.json` 的 `layeredDecision`，保持固定输出目录里的控制面与行为证据同步
- 本机复跑记录、更细的回归测试覆盖，以及旧 fail 收口过程统一下沉到 `07-首轮基线与历史进展归档.md` 与 `08-当前代表集与Gate契约参考.md`（均为参考，当前主线推进**不必须读取**）

当前判定应统一为：**L3 能力已存在，但当前不是目标，也不进入 TODO。** 本页当前只回答“如果后续确实要升级到 L3，当前唯一允许复用的最小自动化边界是什么”。

## 总体策略

### 原则 1：compute-first

最小行为测试优先从 **compute shader** 开始，因为它：

- 容易自动化
- 不依赖窗口/UI
- 输出校验简单（buffer / texture）
- 最适合作为首批行为 oracle

### 原则 2：render-second

只有当某类问题明显属于 vertex/fragment 或 render pipeline 行为时，再补：

- 离屏 render
- 小尺寸 attachment 比较
- 关键顶点/片元输出检查

补充约束：

- render-second 仍应优先视为 **L3 的最小离线扩展**，而不是提前跳到 `SV-005`
- 若新方案需要 GUI、人工登录、工作区外修改或外部 app 介入，就已经越过当前 L3 的自动化边界，不应写回默认流程

### 原则 3：先少量高价值样本

第一阶段不追求覆盖面，而追求：

- 历史容易出错的 lowering 模式
- L2 标为中高风险的样本
- 可以稳定自动执行的样本

## 建议的样本分组

> 注：本节更偏向后续扩样时的分组参考；当前全局主线仍是 L1/L2，因此日常推进不需要按这里批量扩样。

建议继续按以下四类理解旧样本分组：

- A 组：控制流与选择
- B 组：类型与转换
- C 组：内存与地址空间
- D 组：builtin / texture / sampler

更细的历史扩样背景已下沉到 `07-首轮基线与历史进展归档.md`，当前**不必须读取**。

## 第一版实现建议

### Phase 1：compute 行为 harness

当前已落地一版最小 harness，职责为：

1. 读取 `gate-summary.json` / `roundtrip-summary.json`
2. 直接从 `layeredDecision.l3Plan.candidateSampleKeys` 选择第一批 compute 候选
3. 载入 reference MSL 与 round-trip 生成的 MSL
4. 创建 `MTLDevice` 并执行 compute pipeline
5. 读回输出并按 `exact` / `approx` 策略比较
6. 产出结构化 `behavior-summary.json` 与每个样本的中间 artifact

当前入口：

```bash
python3 Scripts/test_ir_semantics_behavior_runner.py
python3 Scripts/ir_semantics_behavior_runner.py --gate-summary build/semantics-validation/roundtrip/test-data-representatives/gate-summary.json
```

当前产物：

- `build/semantics-validation/roundtrip/test-data-representatives/behavior-summary.json`
- `build/semantics-validation/roundtrip/test-data-representatives/behavior-artifacts/<sample>.spec.json`
- `build/semantics-validation/roundtrip/test-data-representatives/behavior-artifacts/<sample>.result.json`

补充约束：

- 当前默认入口必须继续保持为**完全本地、无 UI、无人工介入**的命令链路
- 若某个候选样本必须依赖 live 场景或人工操作，说明它不适合作为当前 L3 默认样本
- 默认输出目录直接复用 `gate-summary.outputRoot`；这样重复执行时，agent 无需人工整理报告落点

### Phase 2：reference/oracle 选择

建议继续按难度分层理解：

- 优先：CPU 可直接算出的样本
- 其次：手写 MSL 作为 reference
- 最后：render 行为作为 reference

补充说明：这套分层当前只保留为参考；若后续没有新的升级决策，不需要继续扩样。

### Phase 3：离屏 render（必要时）

对于 vertex/fragment 样本，建议继续保持：

- 小尺寸 render target（如 `1x1`、`4x4`、`16x16`）
- 固定顶点输入
- 固定纹理/常量
- 对比颜色、深度或关键 attachment

`SV-004D` 当前已落地首个最小 render-second：

- 新增 `Scripts/metal_fragment_behavior_runner.swift`
- 当前只对白名单样本 `test_intrinsic_vector_icmp_zext` 开启
- 固定使用 helper fullscreen triangle + `4x4` `rgba16Float` offscreen target
- 仍保持**单命令、本地、离屏、无 UI、无工作区外修改**

## 浮点比较策略

行为测试不能一律要求 bit-exact。

建议：

- 默认支持 `abs` / `rel` tolerance
- 区分 `strict` 与 `approx` 模式
- 对 fast-math 相关样本单独记录比较策略

原则：

- 不要因为过严阈值制造大量噪声
- 也不要宽松到掩盖真实问题

## 样本来源建议

### 第一优先来源

- `test-data-representatives`
- 仅在当前 L1/L2 主线证据仍不足时，再从其中挑极少量高信息量候选进入 L3
- 如有必要，再从 `test-data-batch` 中补少量新增代表样本

### 第二优先来源

- L2 标出的少量高风险 `ShaderCorpus` 样本

原则：

- 只引入少量、可稳定跑通的真实样本
- 只复用本地已有样本，不把 fresh capture 写成默认依赖
- 不要一上来就把大样本当默认行为测试集

## 与 L2 的衔接规则

默认应当：

- L0/L1 样本：不急着进入 L3
- L2 样本：先做聚类，再按需挑极少量样本进入 L3
- L3 高风险样本：若能最小化，则进入行为测试；若不能最小化，再视情况后置到 L4

补充说明：

- 当前硬默认 gate 的活跃 `L2` 候选与默认 L3 入口细节，统一以下沉到 `08-当前代表集与Gate契约参考.md` 的结构化报告口径为准；本页不再重复展开会漂移的样本计数与旁路快照
- 当前最重要的已经不是继续扩 L3，而是**继续守住这条最小自动化行为边界的稳定性**

## 当前不建议的做法

- 一上来就为全部 `test-data/` 建行为 oracle
- 一上来就做真实 app 的行为验证
- 把大量 GUI/render live 流程塞进 L3
- 让用户手动观察画面当作默认测试方式
- 因缺少现成样本就让用户协助准备环境
- 因为想尽快覆盖 fragment 候选，就直接把 `SV-005` 的流程搬进 L3

## 完成标准

满足以下条件后，可认为 `SV-004` 有了第一阶段结果：

1. 已建立 compute-first 的最小行为 harness
2. 至少有一组样本可以自动执行并比较输出
3. 至少覆盖 1~2 类高风险 lowering 模式
4. 行为测试结果可以结构化落盘
5. 整个流程无需用户频繁介入

当前状态：以上五条已经满足，因此 `SV-004` 的第一阶段交付已经完成；后续若没有新的明确升级决策，就不应为了覆盖率主动扩样。

## 后续扩展

若后续用户明确决定把目标从当前 L1/L2 主线继续升级，再逐步补：

- 更多 oracle 类型
- 更多仍满足 automation-first 契约的 render-second 白名单样本
- 与 L2 风险分级的自动联动

补充约束：后续扩展只有在**仍能保持 agent 自主、单命令、本地、无人工介入**时，才适合纳入默认 L3 路径；否则应保持为后置专项流程。
