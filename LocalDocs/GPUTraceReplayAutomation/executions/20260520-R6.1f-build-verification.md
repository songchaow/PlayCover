# R6.1f 编译验证 — 执行记录

**时间**：2026-05-20 23:52  
**任务**：R6.1f — Makefile + ad-hoc 签名 + 集成测试  
**状态**：✅ 完成

---

## 产出文件

| 文件 | 说明 |
|------|------|
| `Scripts/Makefile` | 编译 + ad-hoc 签名 + test 目标 |
| `Scripts/test_gputrace_replay_bridge.sh` | 集成测试脚本（17 个断言） |

---

## 编译结果

```
clang -O2 -Wall -Wextra -Wno-unused-parameter -fobjc-arc \
      -framework Foundation -framework Metal -ldl -lobjc \
      -o gputrace_replay_bridge gputrace_replay_bridge.m
```

- **错误**：0
- **警告**：4（`-Warc-performSelector-leaks`，ARC 对 runtime selector 的无害警告）+ 1 linker 重复 `-lobjc` 警告
- **签名**：`codesign --force --sign "-" gputrace_replay_bridge` ✅

---

## 集成测试结果

```
=== gputrace_replay_bridge Integration Tests ===

[T1] No arguments → exit code 1
  [PASS] exit code = 1
[T2] help subcommand
  [PASS] exit code = 0
  [PASS] JSON has 'tool' field
  [PASS] JSON has 'commands' array
  [PASS] JSON has 'version' field
  [PASS] commands contains 'help'
  [PASS] commands contains 'replay'
  [PASS] commands contains 'pipeline'
  [PASS] commands contains 'shader'
  [PASS] commands contains 'config'
[T3] Unknown command → exit code 1
  [PASS] exit code = 1
[T4] Subcommands without required args → exit code 1
  [PASS] replay no-args → exit 1
  [PASS] pipeline no-args → exit 1
  [PASS] shader no-args → exit 1
  [PASS] config no-args → exit 1
[T5] replay with invalid path → exit code 2
  [PASS] exit code = 2 (bad input)
[T6] codesign verification
  [PASS] codesign valid

=== Results: 17 passed, 0 failed, 17 total ===
```

---

## Makefile 用法

```bash
cd Scripts/

# 编译 + 签名
make

# 仅编译检查
make check

# 运行集成测试
make test

# 清理
make clean

# 带 live trace 测试
GPUTRACE_PATH=/path/to/sample.gputrace make test
```

---

## 测试覆盖

| # | 测试项 | 验证点 |
|---|--------|--------|
| T1 | 无参数 | 退出码 = 1 |
| T2 | help 子命令 | 退出码 = 0, JSON 含 tool/commands/version，5 个子命令全列出 |
| T3 | 未知命令 | 退出码 = 1 |
| T4 | 各子命令无参数 | replay/pipeline/shader/config 均退出 1 |
| T5 | 无效路径 | 退出码 = 2 (bad input) |
| T6 | codesign | ad-hoc 签名有效 |
| T7 | Live trace (可选) | replay/pipeline/config 全路径验证 |

---

## 结论

R6.1f 全部完成：
- Makefile 提供标准构建流程（`make` / `make test` / `make clean`）
- ad-hoc 签名通过 `CODE_SIGN_IDENTITY="-"` 实现
- 集成测试 17/17 通过，覆盖退出码、JSON 格式、签名验证
- 可选 live trace 测试通过 `GPUTRACE_PATH` 环境变量启用
