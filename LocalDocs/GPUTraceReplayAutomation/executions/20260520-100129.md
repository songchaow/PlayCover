## 本次执行概览

- **时间戳**：`20260520-100129`
- **目标**：为 `GPUTraceReplayAutomation` 建立可持续维护的 dashboard 文档体系，并把当前静态/动态扫描结论沉淀成后续可接力执行的基线。
- **本次是否实现新功能**：否。本次只建立文档与任务方向，不实现 bridge 或 replay 调用代码。

## 本次做了什么

- 创建主 dashboard：`README.md`
- 创建细节子文档：`subdocs/20260520-replay-entry-scan.md`
- 创建本次 execution 文档：`executions/20260520-100129.md`
- 复核并补充当前活动 replay 的动态观察结果

## 本次关键发现

- 当前活动 replay 中，除 `Xcode` 外，还存在：
  - `GPUToolsCompatService`
  - `GPUToolsAgentService`
  - `GPUToolsReplayService.xpc`
  - `GTLLVMHelper`
- `GPUToolsReplayService` 直接读取：
  - `.gputrace/store0`
  - `GPUToolsReplay.framework/Resources/default.metallib`
  - `com.apple.gputools.GPUToolsReplayService/com.apple.metal/.../functions.*`
  - `com.apple.gputools.GPUToolsReplayService/com.apple.metal/.../libraries.*`
- 已确认以下锚点值得进入下一轮逆向：
  - `com.apple.gputools.MTLReplayer`
  - `com.apple.gputools.replay`
  - `-[DYCaptureSession _activateWithSession:serial:invalidationCompletion:initiatedByInferior:replayerLaunchDictionary:]`
  - `-[DYGuestAppSession hardwareCountersConfiguration]`
  - `-[DYDevice needsGPUToolsServiceBeforePlayback]`

## 为什么当前不直接做 bridge

- 还没拿到最小对象图；现在直接写 bridge，接口大概率不稳。
- 当前最值得先做的是 `R1.1`：把 `GPUToolsServices` / `GPUToolsReplayService` 的类与 selector 系统化导出。
- 等 `R1.1` 完成，再决定 bridge 应该优先做：
  - 只读扫描代理；还是
  - ObjC 私有调用薄封装；还是
  - 围绕 XPC / service 的代理层。

## 本次未做的事

- 未修改任何源码逻辑。
- 未 attach / 注入 / 替换任何 Xcode 或系统文件。
- 未尝试触发新的 replay。
- 未实现客户端 bridge。

## 产出文件

- `README.md`
- `subdocs/20260520-replay-entry-scan.md`
- `executions/20260520-100129.md`

## 对下一位 agent 的提醒

- 直接从 `README.md` 中的 `R1.1` 开始。
- 不要跳去实现 bridge。
- 当前最值钱的证据已经拿到：`GPUToolsReplayService` 是活的，并且直接吃 `.gputrace/store0` 和 replay 缓存文件。
