## 首轮基线与历史进展归档

## 文档职责

本文件只保留 `SemanticsValidation` 首轮落地阶段的**历史归档信息**，包括：

- 早期文档与实现收口过程
- 较早批量快照
- 对后续仍有参考价值的旧样本背景

**本页不是任务控制面。任何当前状态、进展、优先级、TODO 与完成判定，一律以 `00-Dashboard.md` 为准。**

## 历史里程碑归档

下表只记录历史结论，便于回溯，不用于表达当前执行面：

| 里程碑 | 历史结论 |
|---|---|
| `SV-000` | 完成现状调研与缺口定稿 |
| `SV-001` | 完成离线 round-trip harness |
| `SV-002` | 完成 canonical compare 与风险分级 |
| `SV-003A ~ SV-003D` | 完成代表 preset、manifest 与单一来源收口 |
| `SV-006` | 完成分层 gate 与 `layeredDecision` 语义 |
| `SV-004A ~ SV-004E` | 完成 compute harness、render-second 准入与相关样本收口 |

## 关键提交脉络

按时间顺序，首轮关键节点包括：

1. `65b30fcf` — `docs: add semantics validation dashboard and plan`
   - 建立 `00-06` 主文档骨架
   - 整理 L1/L2/L3/L4 分层模型
2. `a6abbc50` — `Add IR semantics roundtrip runner`
   - 新增 `Scripts/ir_semantics_roundtrip_runner.py`
   - 打通 `IR -> MSL -> AIR -> IR` 离线 round-trip 主链路
3. `79259c61` — `Add IR canonical compare and risk grading`
   - 新增 `Scripts/ir_canonical_compare.py`
   - 让 `roundtrip runner` 自动产出 `compare-summary.json / risk-report.json / high-risk-samples.json`
4. 随后一轮收口 — `Extend semantics runner with preset manifest and replay baseline snapshots`
   - 为 `Scripts/ir_semantics_roundtrip_runner.py` 新增 `--baseline-report` / `--save-baseline` / `--manifest-file`
   - 固定输出目录开始稳定产出 `preset-manifest.json`
5. 后续补足
   - `gate-summary.json` 稳定产出 `layeredDecision`
   - 行为报告开始以结构化产物形式回写到固定目录

## `test-data-batch` 早期批量基线

较早的批量目录位于：

- `build/semantics-validation/roundtrip/test-data-batch/`

这份快照保留的是首轮批量结果，用于回答历史问题，例如：

- 第一版 compare 的噪声面主要分布在哪里
- 哪些模式后来被代表集、降噪策略与 gate 语义收敛掉了
- 哪些旧样本曾经扮演过 blocker 或代表样本角色

它适合作为**历史对照**，不适合作为动态控制面的事实来源。

### 首轮代表样本名单

这份较早快照中保留过的代表分组包括：

- **L0**：`test_fast_math_binary`
- **L1**：`test_int_literal_half_suffix`、`test_scalar_select_vector`、`test_vector_select_global_gep`
- **L2**：`test_casts`、`test_fast_math_select`、`test_intrinsic_vector_icmp_zext`
- **compile blocker**：`test_struct_array_field`

这些样本名继续保留历史对照价值，但不单独表达当前动态状态。

## 仍有参考价值的旧证据

### 1. 旧 fail evidence

作为历史对照，以下旧问题仍值得保留：

- `test_casts`：曾用于定位 `IRToMSLConverter` 对 `air.convert.u.*` / `air.convert.*.u.*` 的 unsigned 语义恢复问题
- `test_intrinsic_vector_icmp_zext`：曾用于识别 reference sample 与 `.ll` 输出语义漂移带来的 sample oracle 噪声

### 2. 早期落地产物

仍值得保留的早期产物包括：

- `build/semantics-validation/roundtrip/explicit-smoke/`
- `build/semantics-validation/roundtrip/test-data-batch/`

### 3. 早期结论

首轮结果已经足以说明：

- compile green 不能替代语义验证
- 文本 diff 不能替代结构化 compare
- 直接跳到 live 并不是最好的默认起点

## 使用建议

- 看动态控制面：`00-Dashboard.md`
- 看分层路线：`02-总体技术路线.md`
- 看历史样本与首轮提交脉络：回到本文件
- 看 preset / gate / manifest 契约：`08-当前代表集与Gate契约参考.md`
