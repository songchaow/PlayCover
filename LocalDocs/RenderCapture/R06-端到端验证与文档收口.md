### 任务编号与标题

- **ID**：`R06`
- **标题**：端到端验证与文档收口

### Dashboard

- **状态**：`TODO`
- **优先级**：`P2`
- **预计工作量**：`M`（中型）
- **依赖任务**：`R01` ~ `R05`
- **阻塞任务**：无
- **建议执行顺序**：第 6 个（最后执行）
- **预期提交数**：`1`

### 开工前必读

- `00-主文档.md`
- 所有任务卡（确认 R01-R05 均已完成）
- `99-经验教训.md`
- 本文档

### 任务目标

对整个 Metal 截帧功能进行端到端验证，修复发现的问题，更新所有文档，确保功能完整可用。

### 范围内

1. **端到端验证**
   - 从安装 app → 启用 Metal Capture → 启动 app → 通过 MCP 触发截帧 → 检查 `.gputrace` 文件
   - 记录完整的测试步骤和结果
2. **问题修复**
   - 修复验证中发现的任何问题
3. **MCP SettingsTools 更新**
   - 确认 `metalCaptureEnabled` 字段出现在 `update_app_settings` 的 schema description 中
   - 确认 `SettingsService` 正确处理新字段
4. **文档收口**
   - 更新所有任务卡状态为 DONE
   - 更新主文档任务看板
   - 整理最终的经验教训
5. **可选：MCP 测试更新**
   - 如果 SettingsServiceTests 中的 mock 数据需要更新新字段

### 范围外

- 不做大规模重构
- 不添加全新功能

### 预期改动文件

- `PlayCoverMCP/Tools/Host/SettingsTools.swift`（如需更新 schema description）
- `PlayCoverMCP/HostServices/Settings/SettingsService.swift`（如需更新）
- `PlayCoverMCPTests/SettingsServiceTests.swift`（如需更新 mock）
- `PlayCoverMCPTests/SettingsToolsAndResourcesTests.swift`（如需更新）
- `LocalDocs/RenderCapture/*`（所有文档）

### 端到端测试步骤

#### 场景 1：基本截帧流程

1. 在 PlayCover 中安装一个使用 Metal 的 iOS app
2. 检查 app 的 Info.plist 包含 `MetalCaptureEnabled = YES`
3. 在设置中启用 `metalCaptureEnabled`
4. 启动 app
5. 通过 MCP `capture_metal_frame` 工具触发截帧
6. 检查 `.gputrace` 文件生成
7. 尝试用 Xcode 打开 `.gputrace` 文件

#### 场景 2：截帧状态查询

1. 启动一个配置了 Metal Capture 的 app
2. 通过 MCP `get_capture_status` 查询状态
3. 确认返回 `available: true, enabled: true`

#### 场景 3：未启用时的行为

1. 安装 app 但不启用 `metalCaptureEnabled`
2. 启动 app
3. 通过 MCP `get_capture_status` 查询状态
4. 确认返回 `enabled: false`
5. 尝试 `capture_metal_frame`，确认返回合理的错误信息

#### 场景 4：GUI 设置界面

1. 打开 app 设置
2. 确认 Metal Capture Toggle 可见且可操作
3. 切换开关，确认设置保存
4. 重启 PlayCover，确认设置保持

### SettingsTools Schema 更新

确认 `SettingsTools.swift` 中的 `update_app_settings` 工具的 schema description 中包含 `metalCaptureEnabled` 字段：

```
"description": "... metalCaptureEnabled (bool), ..."
```

### 验收标准

1. 端到端截帧流程能够成功完成（至少在一个 Metal app 上）
2. `.gputrace` 文件能够被 Xcode 正确打开
3. MCP 工具正确暴露和工作
4. GUI 设置开关正常
5. 所有任务卡状态已更新
6. 经验教训已整理
7. 项目编译通过，MCP 测试通过

### 验证命令

```bash
# 编译验证
xcodebuild -project PlayCover.xcodeproj -scheme PlayCover -configuration Release build
xcodebuild -project PlayCover.xcodeproj -scheme PlayCoverMCP -configuration Release build

# 测试验证
xcodebuild test -scheme PlayCoverMCP -destination 'platform=macOS,arch=arm64'
```

### 实施结果

_（任务完成后由执行 agent 填写）_

- **实际改动文件**：
- **端到端测试结果**：
- **发现的问题与修复**：
- **关键决策**：
- **Commit Hash**：
