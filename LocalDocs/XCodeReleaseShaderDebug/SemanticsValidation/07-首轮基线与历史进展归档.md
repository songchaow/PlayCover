## 首轮基线与历史进展归档

## 目的

本文件用于下沉 `SemanticsValidation` 首轮落地阶段的较旧信息，包括：

- 2026-04-08 当天从“立项文档”推进到“L1/L2 落地”的提交脉络
- `test-data/` 首轮批量结果的详细样本名单
- 仍有参考价值、但不再适合继续占用主 dashboard 篇幅的阶段性说明

主文档当前只保留 active 信息；本文件保留历史上下文与参考锚点，不作为默认入口，也不要求每轮任务都重新阅读。

## 最近关键提交脉络

按时间顺序，当前这条支线的关键节点是：

1. `65b30fcf` — `docs: add semantics validation dashboard and plan`
   - 建立 `00-06` 主文档骨架
   - 把 L1/L2/L3/L4 分层模型正式整理成当前文档体系
2. `a6abbc50` — `Add IR semantics roundtrip runner`
   - 新增 `Scripts/ir_semantics_roundtrip_runner.py`
   - 打通 `IR -> MSL -> AIR -> IR` 离线 round-trip 主链路
   - 首次把 `test-data/` 批量 round-trip 报告纳入当前文档的背景基线
3. `79259c61` — `Add IR canonical compare and risk grading`
   - 新增 `Scripts/ir_canonical_compare.py`
   - 让 `roundtrip runner` 自动继续产出 `compare-summary.json / risk-report.json / high-risk-samples.json`
   - 让本专项从“L1 已有”推进到“L1/L2 均已有第一版自动化能力”
4. 本轮（未单列旧提交号）— `Extend semantics runner with preset manifest and replay baseline snapshots`
   - 为 `Scripts/ir_semantics_roundtrip_runner.py` 新增 `--baseline-report` / `--save-baseline` / `--manifest-file`
   - 固定输出目录现在会继续产出 `preset-manifest.json`
   - `test-data-representatives` 与 `daily-default` 当前都已验证：首轮可保存 baseline，后续同目录复跑会自动复用 `baseline.json` 做 replay diff

## `test-data/` 首轮批量基线

首轮批量目录：

- `build/semantics-validation/roundtrip/test-data-batch/`

首轮概览：

- `27` 个样本中 `26` 个 round-trip 成功
- `1` 个样本在 compile 阶段失败：`test_struct_array_field`
- `llvm-dis` 阶段当前为 `0` 失败
- 风险分布：
  - `L0 = 1`
  - `L1 = 1`
  - `L2 = 5`
  - `L3 = 20`

### 首轮代表样本名单

- **L0**：`test_fast_math_binary`
- **L1**：`test_scalar_select_vector`
- **L2**：
  - `test_casts`
  - `test_fast_math_select`
  - `test_int_literal_half_suffix`
  - `test_intrinsic_vector_icmp_zext`
  - `test_vector_select_global_gep`
- **compile blocker**：`test_struct_array_field`

## `SV-003` 本轮固定入口

### 1. `test-data` 固定代表集

当前已把首轮最有信息量的一组 `test-data` 样本固定为：

- `test_fast_math_binary.ll`
- `test_scalar_select_vector.ll`
- `test_casts.ll`
- `test_fast_math_select.ll`
- `test_int_literal_half_suffix.ll`
- `test_intrinsic_vector_icmp_zext.ll`
- `test_vector_select_global_gep.ll`
- `test_struct_array_field.ll`

对应固定命令：

```bash
python3 Scripts/ir_semantics_roundtrip_runner.py --preset test-data-representatives --allow-failures
```

对应固定输出目录：

- `build/semantics-validation/roundtrip/test-data-representatives/`

本轮结果：

- `8` 个样本中 `7` 个 round-trip 成功
- `1` 个 compile 失败：`test_struct_array_field`
- 风险分布：`L0 = 1 / L1 = 1 / L2 = 5 / L3 = 1`

### 2. 本机 `ShaderCorpus` 固定代表集

当前已固定的首组本机代表模块为：

- `com.miHoYo.Yuanshen`
  - `db41fcfc1517b115d274f867634e00d08301415d450ca94eb04ca95638e41933`（`vertex`）
- `com.papegames.lysk`
  - `6c08f93015cda305e6c675457bab3acfcf7febab294577d27cbe76317e2b1f45`（`vertex`）
  - `2646854687f12045e370b300deefca49e5c1bcd0a22b8755cd4af656bced5348`（`fragment`）
  - `ec0c6f0e72d6fc64daf4d5955cd1ea2cc5e729b0f988b1e857d13bfb54c7f6c3`（`fragment`）
- `com.tencent.tmgp.speedmobile`
  - `1f5e65cd9f685b3673dd6fac9b81e3af82b5a6d436f8f1824919726483c967ad`（`vertex`）

对应固定命令：

```bash
python3 Scripts/ir_semantics_roundtrip_runner.py --preset local-corpus-representatives --allow-failures
```

对应固定输出目录：

- `build/semantics-validation/roundtrip/local-corpus-representatives/`

本轮结果：

- 固定 `5` 个本地模块全部 round-trip 成功
- 风险分布：`L0 = 0 / L1 = 0 / L2 = 1 / L3 = 4`

### 3. 单命令日常入口

当前已新增默认单命令入口：

```bash
python3 Scripts/ir_semantics_roundtrip_runner.py --preset daily-default --allow-failures
```

它当前等于：

- `test-data-representatives`
- `local-corpus-representatives`

补充说明：从当前控制面口径看，`daily-default` 更适合作为**本机已有 `ShaderCorpus` 样本时的增强入口**；跨机器硬默认仍以 `test-data-representatives` 为准。

### 4. 第一版 gate 判定收口

后续在代表 preset 基线上继续新增了：

- `gate-summary.json`
- `--gate-profile`
- `--enforce-gate`

当前代表 preset 的第一版语义是：

- **`PASS`**：结果落在已配置边界内，且没有活跃已知 debt
- **`WARN`**：仅存在已知 `L2 / L3 / compile blocker`，未出现新增回归
- **`FAIL`**：出现新增 round-trip failure、新增 `L3` blocker，或代表集 job 数异常变化

## 历史上下文说明

### 1. 为什么主文档还保留“缺少 L1/L2”的原始表述

主文档中的部分标题来自本专项最初立项时的原始骨架。为避免控制文档结构频繁漂移，这些标题没有完全重命名；但从当前进展回看：

- “没有离线 IR round-trip 验证” → 已由 `SV-001` 关闭
- “没有 canonical compare / 风险分级” → 已由 `SV-002` 关闭

当前 active gap 已经切换到：

- `SV-003`：稳定日常入口
- `SV-006`：升级/止损规则
- `SV-004`：最小行为测试
- `SV-005`：后置真实场景验证

### 2. 为什么不把首轮样本名单继续放在 dashboard 主体

这些信息仍然有价值，但它们更像：

- 第一版批量结果快照
- 风险样本索引
- 后续回归时的历史对照面

而不是当前每天都需要重新阅读的控制面信息。为了让主文档更适合作为 agent 的默认入口，这些细节统一下沉到本文件。

### 3. 当前最该继续补的，不是更多历史描述，而是更稳定的入口

结合当前工具现状，下一步更有价值的是：

- 固定 `test-data` 代表样本集和默认命令
- 固定一组本地已有 `ShaderCorpus` 代表样本
- 明确什么时候应该停在 L2，什么时候再升级到 L3/L4

## 仍然值得保留的早期落地结果

- 显式单样本 smoke：`build/semantics-validation/roundtrip/explicit-smoke/`
- 首轮 `test-data/` 批量结果：`build/semantics-validation/roundtrip/test-data-batch/`
- 第一版风险分级落地已经足以证明：
  - compile green 不能替代语义验证
  - 文本 diff 不能替代结构化 compare
  - 直接跳 live 不是当前最优技术路线

## 使用建议

- **默认阅读顺序**：`00-Dashboard.md` → `02-总体技术路线.md` → 具体层级文档
- **需要历史样本和进展细节时**：回到本文件
- **需要看更大的背景优先级和当前 live blocker 时**：回到相关背景文档
