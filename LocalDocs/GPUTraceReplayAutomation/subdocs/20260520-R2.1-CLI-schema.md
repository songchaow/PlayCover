# R2.1 技术参考：gputrace_bridge.py（早期只读 bridge，已被 R6.1 取代）

**状态**：✅ 历史完成 — 功能已被 `Scripts/gputrace_replay_bridge.m`（R6.1 ObjC bridge）全面取代  
**保留原因**：Python 扫描脚本仍可用于辅助诊断

---

## 概述

早期 Python 3.9+ 标准库实现的只读 bridge，3 个子命令：

| 子命令 | 功能 | 现状 |
|--------|------|------|
| `scan-active-replay` | 观察 replay 活跃进程 | 仍可用于辅助诊断 |
| `scan-binaries` | 扫描 Xcode GPU 私有模块 | 仍可用于静态分析 |
| `inspect-gputrace` | 检查 .gputrace bundle metadata | 仍可用 |

## 关键设计决策

- 纯 Python 标准库，单文件 `Scripts/gputrace_bridge.py`
- `--json` / `--pretty` / `--dry-run` 公共选项
- 错误码体系：`NO_REPLAY_ACTIVE`, `XCODE_NOT_FOUND`, `MODULE_NOT_FOUND`, `GPUTRACE_INVALID` 等

## 验证结论（R2.3）

9/9 测试 PASS，包括 metadata 解析、符号发现、进程分类。  
动态观察确认的 replay 进程链：Xcode → GPUToolsCompatService → GPUToolsAgentService → GPUToolsReplayService → GTLLVMHelper
