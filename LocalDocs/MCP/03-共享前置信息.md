### 仓库事实

- **仓库根目录**：`/Users/songdogwang/Codes/PlayCover`
- **当前规划基线分支**：`develop`
- **Xcode 工程**：`PlayCover.xcodeproj`
- **现有 app target**：`PlayCover`
- **现有 scheme**：`PlayCover`
- **当前未发现现成测试 target**
- **当前 `LocalDocs/MCP` 是本轮规划文档根目录**

### `xcodebuild -list` 已确认结果

当前工程已确认：

- **Targets**：`PlayCover`、`PlayCoverMCP`、`PlayCoverMCPTests`
- **Build Configurations**：`Release`、`Nightly`
- **Schemes**：`PlayCover`、`PlayCoverMCP`

### 现有源码能力锚点

#### Host 能力

- **安装 / 导出**：`PlayCover/AppInstaller/Installer.swift`
- **应用启动 / LLDB 启动 / 重签名 / 清 PlayChain**：`PlayCover/Model/PlayApp.swift`
- **卸载 / 清理**：`PlayCover/Views/Uninstaller.swift`
- **`PlayTools` 注入与移除**：`PlayCover/Utils/PlayTools.swift`
- **entitlements**：`PlayCover/Utils/Entitlements.swift`
- **shell / codesign / lldb**：`PlayCover/Utils/Shell.swift`
- **应用设置**：`PlayCover/Model/AppSettings.swift`
- **应用信息 / category**：`PlayCover/Model/AppInfo.swift`
- **应用枚举**：`PlayCover/ViewModel/AppsVM.swift`
- **keymap**：`PlayCover/Utils/Keymapping.swift`

#### Session 能力

- **fake touch 原语**：`Carthage/Checkouts/PlayTools/PlayTools/Controls/PTFakeTouch/PTFakeMetaTouch.*`
- **触控派发**：`.../Controls/Backend/Toucher.swift`
- **鼠标转触控**：`.../TouchscreenMouseEventAdapter.swift`
- **动作层**：`.../Controls/Backend/Action/PlayAction.swift`
- **调试叠加层**：`.../Controls/Backend/DebugOverlay/DebugController.swift`

### 现状判断

#### 已有

- Host 侧大多数能力都已经存在业务实现
- Session 侧输入原语已经存在
- `launch with LLDB` 已有

#### 缺失

- MCP server 本体
- 可测试的服务层
- Session host/runtime bridge
- 测试 target
- 统一的 task / logging / error model
- 运行中 attach debugger

### 推荐构建命令

#### 当前可用

- **列出工程信息**
  - `xcodebuild -list -project PlayCover.xcodeproj`
- **构建主 app**
  - `xcodebuild -project PlayCover.xcodeproj -scheme PlayCover -configuration Release build`

#### 规划中，待 H00 / H01 完成后可用

- **构建 MCP server**
  - `xcodebuild -project PlayCover.xcodeproj -scheme PlayCoverMCP -configuration Release build`
- **运行 MCP 测试**
  - `xcodebuild test -project PlayCover.xcodeproj -scheme PlayCoverMCP -destination 'platform=macOS'`
- **执行 MCP 冒烟脚本**
  - 由 `H00` 建立统一脚本入口，例如 `Scripts/mcp_smoke.py`

### 对 agent 的约束提醒

- 不要假设仓库里已经有 MCP 相关目标
- 不要假设仓库里已经有测试基础设施
- Session 相关任务会触碰 `PlayTools` 注入侧代码，要格外控制改动范围
- 对于需要真实 IPA / 已安装 app 的测试，优先补 fake / fixture，再补 smoke

### 文档真相源

后续实施时，以以下文档为准：

- **范围与架构**：`01-总体规划与架构.md`
- **测试要求**：`04-测试与验证策略.md`
- **提交要求**：`05-提交与文档更新规范.md`
- **任务状态**：`06-全局任务看板.md`
- **任务边界**：具体任务卡
