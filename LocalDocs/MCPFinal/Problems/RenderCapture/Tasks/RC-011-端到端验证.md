## RC-011：端到端验证延迟注入方案

### 一、任务目标

重建 PlayTools xcframework + PlayCover.app，在真实 app 上实测：

- **Q3**：QQ飞车延迟注入后能否成功截帧？
- **Q2**：原神 `metalCaptureEnabled=true` 延迟注入后能否正常启动（不崩溃）？

### 二、状态

**DONE** — 2026-03-31 23:18 全部验证通过。

### 三、执行步骤与结果

#### 3.1 重建 PlayTools xcframework

```bash
FORCE_PLAYTOOLS_REBUILD=1 ./BuildScripts/sync_playtools_xcframework.sh Release
```

结果：✅ BUILD SUCCEEDED，产物已同步到 `Carthage/Build/PlayTools.xcframework/ios-arm64/PlayTools.framework`。

#### 3.2 重建并安装 PlayCover.app

```bash
./BuildScripts/build_and_install.sh Release
```

结果：✅ BUILD SUCCEEDED，安装到 `~/Applications/PlayCover.app`，ad-hoc 签名验证通过。

#### 3.3 QQ飞车截帧验证（Q3）

1. `remove_playtools` → `inject_playtools`：重新注入最新 PlayTools
2. `launch_app`：✅ 启动成功
3. `create_session`：✅ status=ready（PID 96709）
4. `get_capture_status`：✅ `supports_gpu_trace=true`，`tracked_command_queue_count=5`
5. `capture_metal_frame`：✅ 成功启动 capture

**产物**：`/Users/songdogwang/Library/Containers/com.tencent.tmgp.speedmobile/Data/Documents/Captures/capture_20260331_231639.gputrace`（**413MB**）

关键 diagnostic_summary：
```
enabled=true, captureManagerAvailable=true, supportsGPUTrace=true,
supportsDeveloperTools=false, hasDefaultDevice=true, defaultDeviceName=Apple M4 Pro,
queueDiscoveryInstalled=true, trackedCommandQueues=5,
latestTrackedQueue=class=CaptureMTLCommandQueue, label=nil, device=Apple M4 Pro,
source=newCommandQueue, discoveries=1, failureReason=none
```

#### 3.4 原神正常启动验证（Q2）

1. `update_app_settings`：设置 `metalCaptureEnabled=true`
2. `remove_playtools` → `inject_playtools`：重新注入最新 PlayTools
3. `launch_app`：✅ 启动成功（PID 98638）
4. 等待 10 秒后确认进程存活：✅ 进程仍在运行
5. `create_session`：✅ status=ready

**对比**：在旧的 `DYLD_INSERT_LIBRARIES` 方案下，原神在相同配置下启动即崩溃（`SIGABRT`）。

### 四、结论

延迟注入方案（RC-009）端到端验证**全部通过**：

| 验证项 | 结果 |
|---|---|
| QQ飞车延迟注入截帧（Q3） | ✅ 413MB `.gputrace` 成功生成 |
| 原神 `metalCaptureEnabled=true` 正常启动（Q2） | ✅ 无崩溃，session ready |
| PlayTools 重建流程 | ✅ 标准脚本正常工作 |
| PlayCover.app 重建安装流程 | ✅ 标准脚本正常工作 |

### 五、技术说明

- `ensureGPUToolsCaptureLoaded()` 符号在注入到 app 的 PlayTools 二进制中已确认存在（通过 `nm` 验证）
- `inject_playtools` 从 `PlayCover.app/Contents/Frameworks/PlayTools.framework` 复制到 `~/Library/Frameworks/PlayTools.framework`
- 二进制与 xcframework ios-arm64 slice MD5 不完全一致（因 codesign 差异），但符号表一致（`ensureGPUToolsCaptureLoaded` 偏移地址相同 `0x16bfc`）
