## Render Capture：验证入口与代码锚点

### 一、当前已知验证入口

#### 1. GUI / MCP 基础入口

- `PlayCover.app`
- GUI 内嵌 MCP HTTP：`http://127.0.0.1:19820/mcp`
- runtime 注册端口：`52741`

#### 2. 推荐验证层次

优先按下面顺序理解问题：

1. GUI / MCP 是否正常启动
2. 真实 app 是否能 `launch_app`
3. `create_session` 是否到 `ready`
4. `get_capture_status` 的四个关键字段分别是什么
5. `capture_metal_frame` 是否真的落盘 `.gputrace`

#### 3. 当前真实测试对象

- 主对象：`QQ飞车`（`com.tencent.tmgp.speedmobile`）
- 对照候选：`原神`（`com.miHoYo.Yuanshen`）

---

### 二、关键 MCP 工具

与本问题最相关：

- `launch_app`
- `create_session`
- `list_sessions`
- `get_capture_status`
- `capture_metal_frame`
- `get_app_settings`
- `update_app_settings`
- `check_playtools_installed`

---

### 三、关键文档

优先阅读：

- `LocalDocs/MCPFinal/03-能力清单.md`
- `LocalDocs/MCPFinal/04-接入与验证.md`
- `LocalDocs/MCPFinal/01-架构与设计.md`

其中对本问题最关键的现有结论是：

- `capture_metal_frame` 依赖 session 链路
- `metalCaptureEnabled` 必须开启
- 文档明确提示：修改后需要重新安装 app

---

### 四、关键代码锚点

#### 1. runtime / PlayTools 侧

- `Carthage/Checkouts/PlayTools/PlayTools/MetalCaptureService.swift`
  - `initialize()`
  - `captureFrame(...)`
  - `getStatus()`

- `Carthage/Checkouts/PlayTools/PlayTools/Controls/Backend/Bridge/BridgeListener.swift`
  - `capture_frame`
  - `get_capture_status`

#### 2. MCP bridge / tool 侧

- `PlayCoverMCP/Session/CaptureService.swift`
- `PlayCoverMCP/Tools/Session/CaptureTools.swift`
- `PlayCoverMCP/Session/BridgeClient.swift`

#### 3. 安装与设置侧

- `PlayCover/AppInstaller/Installer.swift`
- `PlayCover/Model/AppSettings.swift`
- `PlayCover/Views/AppSettingsView.swift`

---

### 五、当前最关键的已知事实

截至当前，已知：

- `QQ飞车` 能成功创建 `ready` session
- `metalCaptureEnabled=true`
- 已安装 app 的 `Info.plist` 中 `MetalCaptureEnabled=true`
- `get_capture_status` 返回：
  - `available=true`
  - `enabled=true`
  - `supports_gpu_trace=false`
- 实际 `capture_metal_frame` 失败，且无 `.gputrace` 落盘

详细背景请看：

- `Tasks/RC-001-查清-supports_gpu_trace_false.md`
