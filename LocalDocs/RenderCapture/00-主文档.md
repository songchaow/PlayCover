## Metal 截帧功能（RenderCapture）

### 目标

为 PlayCover 添加对运行中 iOS app 的 **Metal GPU 截帧**能力，允许用户触发一次截帧操作，将 `.gputrace` 文件导出到指定路径，随后可用 Xcode 打开进行渲染流程分析。

### 技术路线

**路径1：Info.plist + MTLCaptureManager API 编程式截帧**

在 app 的 Info.plist 中声明 `MetalCaptureEnabled = YES`，然后在 PlayTools dylib 运行时中通过 `MTLCaptureManager.shared()` 的 API 进行编程式截帧，将 `.gputrace` 文件写入磁盘。

### 架构概览

```
PlayCover Host App                      PlayTools (注入到目标 app 进程)
┌─────────────────────┐                ┌──────────────────────────────────┐
│ AppSettings          │  共享 plist   │ PlaySettings                     │
│ ├ metalCaptureEnabled├──────────────►│ ├ metalCaptureEnabled            │
│                      │               │                                  │
│ Installer            │               │ MetalCaptureService              │
│ ├ 写 Info.plist      │               │ ├ MTLCaptureManager.startCapture │
│ │  MetalCaptureEnabled│              │ ├ MTLCaptureManager.stopCapture  │
│                      │               │ ├ 导出 .gputrace                 │
│ PlayApp              │               │                                  │
│ ├ launch() 条件保留  │               │ BridgeListener                   │
│ │  Metal 环境变量     │               │ ├ handleCommand("capture_frame") │
│                      │               └──────────────────────────────────┘
│ PlayCoverMCP         │                         ▲
│ ├ capture_metal_frame│  Bridge TCP             │
│ ├ get_capture_status ├─────────────────────────┘
│                      │
│ AppSettingsView      │
│ ├ Metal Capture 开关 │
└─────────────────────┘
```

### 关键技术要点

#### 1. Info.plist `MetalCaptureEnabled`

- macOS 上的 Catalyst 应用如果要使用 `MTLCaptureManager` 的编程式截帧，**必须**在 Info.plist 中设置 `MetalCaptureEnabled = YES`
- 这个 key 在安装时写入（Installer.swift），类似现有的 `MinimumOSVersion` 修改
- **注意**：此 key 仅在 app 启动时读取，修改后需要重新安装或重签名才能生效

#### 2. 环境变量清除机制

- `PlayApp.swift` 的 `clearDebugAffectingEnvironment()` 和 `runAppExec()` 会在启动前清除 13 个 Metal 相关环境变量
- 包括 `METAL_CAPTURE_ENABLED`、`METAL_CAPTURE_OUTPUT_FILE` 等
- **对于编程式截帧（使用 MTLCaptureManager API），Info.plist 的 `MetalCaptureEnabled` 足够，不需要环境变量**
- 因此环境变量清除机制**不需要修改**——编程式截帧不依赖环境变量

#### 3. PlayTools 侧截帧实现

- PlayTools 运行在目标 app 进程内（作为注入的 dylib），能够直接访问 Metal API
- `MTLCaptureManager.shared()` 可以获取截帧管理器
- `supportsDestination(.gpuTraceDocument)` 检查是否支持导出 `.gputrace`
- `MTLCaptureDescriptor` 设置截帧范围（captureObject 传 device 或 commandQueue）和输出 URL
- `startCapture(with:)` 开始截帧，`stopCapture()` 结束截帧
- 截帧覆盖一帧的 GPU 命令提交

#### 4. 设置共享机制

- PlayCover Host 和 PlayTools 通过共享 plist 文件通信：
  `~/Library/Containers/io.playcover.PlayCover/App Settings/{bundleId}.plist`
- 新增 `metalCaptureEnabled` 字段到 `AppSettingsData`
- PlayTools 侧的 `AppSettingsData` 需要同步新增该字段

#### 5. 沙箱与文件访问

- 目标 app 运行在 app-sandbox 下
- `.gputrace` 输出路径需要在沙箱可写范围内
- 推荐输出到 app 的 container 目录或 `~/Library/Containers/io.playcover.PlayCover/` 下

### 仓库事实

- **仓库根目录**：`/Users/songdogwang/Codes/PlayCover`
- **当前基线分支**：`develop`
- **文档根目录**：`LocalDocs/RenderCapture/`
- **PlayCover Host 代码**：`PlayCover/`
- **PlayTools 代码**：`Carthage/Checkouts/PlayTools/PlayTools/`（被 .gitignore 忽略，修改通过 Patch 管理）
- **PlayTools Patch 目录**：`Patches/`（存放 PlayTools 的 patch 文件，纳入 git）
- **Patch Apply 脚本**：`Patches/apply_playtools_patches.sh`（Carthage bootstrap 后执行）
- **MCP 代码**：`PlayCoverMCP/`
- **测试代码**：`PlayCoverMCPTests/`

### 构建与测试命令

- **构建主 app**：
  `xcodebuild -project PlayCover.xcodeproj -scheme PlayCover -configuration Release build`
- **构建 MCP**：
  `xcodebuild -project PlayCover.xcodeproj -scheme PlayCoverMCP -configuration Release build`
- **运行 MCP 测试**：
  `xcodebuild test -project PlayCover.xcodeproj -scheme PlayCoverMCP -destination 'platform=macOS,arch=arm64'`

### 任务看板

| ID | 标题 | 状态 | 依赖 | 优先级 |
|----|------|------|------|--------|
| R01 | PlayCover Host 设置与安装管线改动 | `DONE` | 无 | P0 |
| R02 | PlayTools MetalCaptureService 核心实现 | `TODO` | R01 | P0 |
| R03 | PlayTools Bridge 截帧命令处理 | `TODO` | R02 | P1 |
| R04 | MCP 截帧工具暴露 | `TODO` | R03 | P1 |
| R05 | GUI 设置界面 Metal Capture 开关 | `TODO` | R01 | P2 |
| R06 | 端到端验证与文档收口 | `TODO` | R01~R05 | P2 |

### 任务文档索引

- `R01-Host设置与安装管线改动.md` — PlayCover Host 侧改动
- `R02-PlayTools-MetalCaptureService.md` — PlayTools 截帧核心实现
- `R03-Bridge截帧命令处理.md` — Bridge 层新增截帧命令
- `R04-MCP截帧工具.md` — MCP 暴露截帧 tool
- `R05-GUI设置界面.md` — GUI Metal Capture 开关
- `R06-端到端验证与文档收口.md` — 集成测试与收口
- `99-经验教训.md` — 实施过程中的经验教训

### Agent 工作流程

#### 开工前

1. 读取本文档（`00-主文档.md`）
2. 读取本次任务卡
3. 确认 `git status` 干净、分支正确
4. 确认依赖任务已完成

#### 实施中

- 一次只处理一个任务
- 遵守任务卡中的范围边界
- 小步验证，每次改动后构建确认

#### 收工前

1. **验证与测试**：尽量对当前任务实现的功能做验证（构建、单元测试、手动冒烟测试）
2. **更新任务状态**：修改本文档的任务看板和任务卡中的状态
3. **记录实现情况**：在任务卡中简要记录实际改动文件和关键决策
4. **整理经验教训**：如有新教训，追加到 `99-经验教训.md`
5. **Git commit**：`git add` 所有改动并 `git commit`，commit message 格式：`feat(render-capture): R0X - 任务标题简述`

### 注意事项

1. **PlayTools 是 Carthage 依赖**：`Carthage/Checkouts/PlayTools/` 目录下的修改需要谨慎，它是通过 Carthage 管理的。修改 PlayTools 代码后需要确保 framework 能正确编译
2. **pbxproj 修改风险**：如果需要新增文件到 Xcode 项目，参考 `LocalDocs/MCP/08-经验教训与常见陷阱.md` 中的经验教训
3. **PlayCoverMCP 不能导入 PlayCover 模块**：MCP target 是独立的 CLI tool，不链接 PlayCover framework
4. **两份 AppSettingsData**：PlayCover Host 侧和 PlayTools 侧各有一份 `AppSettingsData` 定义，新增字段时两边都要同步
5. **沙箱限制**：目标 app 运行在沙箱中，文件 I/O 路径需要在沙箱允许范围内
6. **编程式截帧不需要环境变量**：`MTLCaptureManager` API 只需要 Info.plist 的 `MetalCaptureEnabled = YES`，不依赖 `METAL_CAPTURE_ENABLED` 等环境变量，因此**不需要修改** `clearDebugAffectingEnvironment()` 的逻辑
