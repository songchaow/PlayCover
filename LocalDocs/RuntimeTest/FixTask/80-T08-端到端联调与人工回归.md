## T08：端到端联调与人工回归

### 目标

在前序修复完成后，统一收口并关闭 RT01–RT08。  
这是最后的 **真实 GUI / MCP / Runtime 联调与人工回归** 任务。

本任务的重点不是再大改架构，而是：

1. 用真实环境重新跑一遍问题矩阵
2. 证明前序修复已经在真实链路上生效
3. 把最终测试流程沉淀为可复用文档或脚本

---

### 依赖

建议在 `T01`–`T07` 全部完成后执行。

---

### 建议先读

- `LocalDocs/RuntimeTest/00-主文档.md`
- `LocalDocs/RuntimeTest/01-应用与运行时问题.md`
- `LocalDocs/RuntimeTest/02-任务与协议问题.md`
- `LocalDocs/RuntimeTest/03-安装与IPA兼容性问题.md`
- `99-经验教训.md`

---

### 推荐验证矩阵

#### 1. 只读 / 诊断类

- `list_installed_apps`
- `get_app_info`
- `get_app_settings`
- `preview_entitlements`
- `validate_app_signing`
- `list_keymaps`
- `get_keymap`

#### 2. 可逆写操作

- `update_app_settings`
- `create_keymap`
- `rename_keymap`
- `reset_keymap`
- `delete_keymap`

#### 3. task / 协议类

- 标准 task tools 的查询 / 列表 / 取消
- SSE task status 通知
- 长任务失败原因是否能直接通过标准路径拿到

#### 4. runtime / 输入类

- `launch_app`
- `create_session`
- `list_sessions`
- `tap`
- `long_press`
- `swipe`
- `drag`
- `press_key`
- `type_text`
- `toggle_debug_overlay`
- `close_session`

#### 5. 安装链路类

- 对已知无 ARM64 的 IPA 做**负例验证**，确认能更早、更清晰失败
- 如有可用 ARM64 IPA，可再补正例安装 / 卸载回归

---

### 强约束

- **绝对不要卸载 `原神`**
- `QQ飞车` 的卸载 / 安装只能在明确需要时做，并在文档里记录
- 对给定 IPA 的验证优先作为**负例**使用，除非其兼容性已被前序任务重新确认

---

### 推荐工作内容

1. 把最终人工回归流程沉淀成：
   - 文档步骤，或
   - 新增脚本（如果确实合适）
2. 将每个 RT 问题标记为：
   - 已修复
   - 部分修复
   - 非 bug，已改进诊断
   - 仍阻塞
3. 若仍有残留问题，必须明确：
   - 复现条件
   - 当前证据
   - 是否阻塞上线 / 后续 MCP 使用

---

### 验收标准

完成后应满足：

1. `LocalDocs/RuntimeTest` 中的 RT01–RT08 都有最终结论
2. 新流程可被下一位开发者复用，而不是只存在聊天记录里
3. 全量自动化回归通过
4. 真实 GUI / MCP / Runtime 链路至少完成一轮可信验证

---

### 建议测试

先跑自动化全量回归：

```bash
cd /Users/songdogwang/Codes/PlayCover && xcodebuild test -scheme PlayCoverMCP -destination 'platform=macOS,arch=arm64' -derivedDataPath /tmp/PlayCover-DerivedData FASTLANE=1 CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=YES
```

```bash
cd /Users/songdogwang/Codes/PlayCover && WITH_REGRESSION=1 DERIVED_DATA_PATH=/tmp/PlayCover-DerivedData /bin/bash /Users/songdogwang/Codes/PlayCover/Scripts/test_http_mcp.sh
```

然后做人工 / 真实环境回归，并把新增流程写回文档或脚本。

---

### 非目标

- 不建议在本任务中继续做大规模代码重构
- 发现新问题时，可以记录并开后续任务，但不要把本任务无限扩张

---

### 完成后必须更新

- `01-任务状态.md`
- `99-经验教训.md`
- `LocalDocs/RuntimeTest` 下的最终问题文档
- 本文档中的“最终回归结果 / 新增测试流程 / 残留问题”
