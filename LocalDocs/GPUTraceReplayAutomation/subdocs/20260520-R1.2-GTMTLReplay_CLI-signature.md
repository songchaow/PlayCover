# R1.2 技术参考：GTMTLReplay_CLI 签名（已被 R3 完善）

**状态**：✅ 完成（初始分析），后续由 R3 完整验证修正  
**注意**：完整的 Options 布局和能力边界请直接参考 `20260520-R3-headless-replay.md`

---

## 函数签名

```c
int GTMTLReplay_CLI(
    const char *captureArchivePath,          // .gputrace 路径
    GTMTLReplayCLIOptions *options,           // 选项结构体指针（≥0x320 字节）
    void (*completionCallback)(NSData *, NSURL *)  // 完成回调（dead code，从不被调用）
);
```

源文件：`/AppleInternal/.../GPUToolsDevice/GPUTools/GTMTLCapture/replayer/GTMTLReplay_CLI.m`

## 关键结论

- CLI 路径仅做 **replay 健康检查**（返回 0/非 0），不产出数据
- 数据获取需走 Controller 路径（R4.2）
- 完整 Options 偏移表和 APR bootstrap → 见 `20260520-R3-headless-replay.md`

## 环境变量

| 变量名 | 作用 |
|--------|------|
| `ATF_RESULTSDIRECTORY` | 覆盖 saveDestination |
| `MTLOverrideDeviceCreationFlags` | Metal device 创建 flags |
| `GPUMTLOverrideDeviceFamily` | 覆盖 GPU device family |
