## 总体技术路线

## 目标

在不打乱当前 offline-first 工作流的前提下，为“语义验证”新增一条分层、渐进、可止损的测试体系。

核心思想：

- **不追求一步到位**
- **先建立离线证据，再逐步增加行为与真实场景证据**
- **最终做到哪一层，由当时的人力余量决定**

补充约束：

- 当前事实优先级应统一为：`gate-summary.json` > `risk-report.json` > `behavior-summary.json` / `behavior-summary.*.json` > `roundtrip-summary.json` > `preset-manifest.json` 的描述文字；其中 `gate-summary.json` 应优先读取 `status` / `activeKnownDebt` / `improvements` / `layeredDecision` 等结构字段，不要把 `gate profile` 或 `preset` 的旧描述文字直接当成当前控制面事实
- 任何新路线若会把 GUI、人工登录、工作区外修改重新写回日常验证，就不应进入默认主线

## 四层模型

### L1：IR Round-trip 结构验证

目标：建立最小闭环：

```text
IR -> MSL -> AIR -> IR
```

回答的问题：

- 当前链路能不能把 `module.ll` round-trip 回 `regenerated.ll`
- 哪些样本在 round-trip 的编译阶段就失败
- 哪些样本可以继续进入结构比较

这是本专项最优先、最基础的一层。

### L2：Canonical Compare + 风险分级

目标：不再只看文本 diff，而是比较：

- entry 函数集合
- shader 类型
- 参数/返回类型摘要
- 地址空间
- builtin / resource 使用
- CFG 粗结构
- instruction family 统计
- 关键 attribute / fast-math 语义点

输出不应只是“same/diff”，而应分级：

- **L0**：几乎一致 / 低风险
- **L1**：存在非关键差异 / 可接受
- **L2**：存在需要进一步复核或升级验证的差异
- **L3**：关键语义面明显不一致

### L3：最小行为测试

目标：在少量高价值样本上建立“固定输入 → 固定输出”的自动验证。

推进顺序：

1. **compute-first**：最容易自动化
2. **必要时补离屏 render**：覆盖 vertex/fragment 的关键路径

这一层的意义是：

- 把“结构相近”推进到“行为更可信”
- 对历史高风险 lowering 模式形成回归样本

### L4：真实场景验证

目标：在真实 shader / 真实 capture / 真实 app 上做最终后置验证。

这一层默认只在下面任一情况启用：

- L1/L2/L3 已基本收敛，需要真实确认
- 某类问题只有 `.gputrace` / live 渲染结构才能暴露
- 新修复影响 runtime/library replacement 主链路

默认使用自动化脚本 / MCP；若必须人工介入，需先取得用户确认。

## 推进顺序

### 阶段 P0：现状定稿

已完成：

- 现状调研
- 缺口确认
- 分层模型成型

对应：`SV-000`

### 阶段 P1：打通 round-trip 主链路

交付目标：

- 新增离线 round-trip runner
- 先对 `test-data/` 跑通
- 产出第一版结构化 JSON 报告

当前状态：**已完成。**

对应：`SV-001`

### 阶段 P2：把结构比较从“文本 diff”升级为“风险分级”

交付目标：

- 新增 IR canonical summary
- 对 round-trip 结果做风险分类
- 在 `test-data/` 形成首批基线
- 挑少量 `ShaderCorpus` 样本试跑

当前状态：

- `SV-002`：**已完成**
- `SV-003`：**首轮收口已到位，当前转入长期守护**
- `SV-006`：**已完成**

这意味着阶段 P2 的 active work 已经从“继续造入口”切换为“只守住默认入口与机器可读分层边界”，当前主线需要记住的只有四条：

- `test-data-representatives` 继续是**跨机器硬默认 gate**；只要 fresh workspace 能稳定跑通它，就说明当前离线控制面仍可由 agent 自主完成
- `daily-default` / `local-corpus-representatives` 继续只是**本机增强证据**；缺少本地样本时应停在 `WARN` 或退回硬默认入口，不把 fresh capture / 用户协助写回日常流程
- 当前控制面事实应优先读取 `gate-summary.json` / `risk-report.json` 的结构字段；`preset-manifest.json` 与 gate profile 的旧描述文字若滞后，只能当参考
- 更细的 preset、baseline snapshot、job-count 边界与 manifest 合同统一下沉到 `08-当前代表集与Gate契约参考.md`；若只是推进当前主线，**不必须读取**

当前已收口的直接结果可以简记为：硬默认 gate 保持 `8/8` round-trip 成功，活跃 `L2` 收缩为 `test_fast_math_select` / `test_intrinsic_vector_icmp_zext`，`test_casts` 已转入 improvement，`test_struct_array_field` 继续停在 blocked sample，`layeredDecision.overallDecision = promote_l2_candidates_to_l3`。

对应：`SV-002`、`SV-003`、`SV-006`

### 阶段 P3：补最小行为测试

交付目标：

- 建立 compute-first 行为测试 harness
- 选 5~10 个高风险 lowering 模式做首批行为样本
- 对关键样本形成稳定 oracle

当前状态：**ONGOING（当前主线，但仅剩 `SV-004F` 守护面）**。`SV-004` 的第一阶段目标已经完成；现在不再把“继续扩样”当作默认方向，而是只把 `SV-004F` 当作唯一实际执行面。当前主线只保留三条要求：

- 默认执行边界固定为 `test_fast_math_select` 与 `test_intrinsic_vector_icmp_zext`
- 默认期望结果固定为 `pass/pass`，并继续把行为产物写回 `test-data-representatives` 的同一输出目录
- 继续守住 sample oracle 与 `.ll` 的同步性；只要这条纯离线证据链足够，就不要为了覆盖率升级到 `SV-005`

更细的 sample 选择、定向 `test_casts` 复核、artifact 契约与 render-second 细节统一见 `05-L3-最小行为测试.md`；本页只保留路线与止损边界。

对应：`SV-004` / `SV-004F`

### 阶段 P4：收口真实场景验证

交付目标：

- 明确默认代表集
- 明确进入 L4 的前置条件
- 把 `.gputrace` / render-diff / MCP live 使用场景收口为后置 gate

当前状态：**未开始，保持后置。**

对应：`SV-005`

## 预算与止损策略

### 人力余量较低时

优先做到：

- **L1 完整落地**
- **L2 第一版 summary compare**
- **`SV-003` 的 `test-data-representatives` 硬默认 gate 固化**

此时已经能显著提高“语义风险可见度”，也是当前最现实、最高性价比的停点。额外约束是：默认 gate 必须仍可由 agent 自主完成，不能把用户配合、fresh capture 或权限交互带回主路径。

### 人力余量中等时

继续做到：

- 代表性 `ShaderCorpus` 作为**已有本地条件下的增强证据**接入
- 建立明确的升级/止损规则（`SV-006`）
- 从稳定代表集里挑少量样本建立最小 compute 行为测试
- 仅在仍有高价值 fragment 候选时，评估是否要补最小 render-second

这是推荐的“中期完整度”目标。

### 人力余量充足时

再补：

- 更丰富的行为样本
- 离屏 render 行为测试
- 真实场景自动化验证
- 必要时更深一层的 canonical compare / AST compare / ISA 验证

## 与现有工作流的关系

现有主回路仍然是“真实样本采集 → 导出 corpus → 离线 replay / compile / diff → 少量 live 复测”。

本专项新增的是一条**更早暴露语义风险的离线验证支路**：`module.ll → round-trip → canonical compare → risk grading → （必要时）behavior test → （必要时）real-scene validation`。

它不是替代现有 E-005/E-006，而是在更低成本、更自动化的阶段提前暴露风险。

补充约束：

- 若前置任务仍在处理更靠前的运行时 blocker，这里应优先提供更强的离线证据，而不是过早把默认流程切到 live
- `SV-003` 默认只复用本地已有 `test-data/` 与 `ShaderCorpus`；若缺样本，不应自动升级为需要用户介入的 fresh capture 流程
- 若新增 render-second，也应先证明它仍属于本地、离屏、自动化优先的 L3，而不是提前把 L4 的人工成本引回来

## 当前推荐的默认 gate

### 必须优先满足

当改动涉及离线语义验证脚本、preset / gate profile / report contract 或 `IRToMSLConverter` 时，默认优先满足：

- `Scripts/test_ir_canonical_compare.py`、`Scripts/test_ir_semantics_roundtrip_runner.py` 保持通过
- `SV-001` 的 round-trip 能稳定跑 `--preset test-data-representatives --allow-failures --enforce-gate`
- `SV-002` 能对代表 preset 生成结构化 compare 报告
- 代表 preset 能继续产出 `gate-summary.json`，并在 `--enforce-gate` 下仅对新增回归执行阻断
- `daily-default` / `local-corpus-representatives` 需要把 job count 的**硬下界**与**完整代表集**分开表达：缺少部分本地 `ShaderCorpus` 样本时记为 `WARN`，不得直接误判为回归
- 固定输出目录下的代表 preset 会继续产出 `preset-manifest.json`；若目录里已有 `baseline.json`，可自动复用它做 replay baseline diff；manifest 还应显式标出 preset 期望代表集与本次 discovered jobs 的命中 / 缺失 / 意外新增情况
- `test-data-batch` 仍只作为参考批量入口：默认不附带 gate profile，也不应承担日常阻断职责
- 若改动触及 `IRToMSLConverter`，应补一次 `FORCE_PLAYTOOLS_REBUILD=1 ./BuildScripts/sync_playtools_xcframework.sh`

推荐默认顺序：

1. **跨机器硬默认**：先保持 `Scripts/test_ir_canonical_compare.py`、`Scripts/test_ir_semantics_roundtrip_runner.py` 通过，再执行 `python3 Scripts/ir_semantics_roundtrip_runner.py --preset test-data-representatives --allow-failures --enforce-gate`
2. **当前默认行为证据**：执行 `python3 Scripts/test_ir_semantics_behavior_runner.py` 与 `python3 Scripts/ir_semantics_behavior_runner.py --gate-summary build/semantics-validation/roundtrip/test-data-representatives/gate-summary.json`
3. **本机已有 `ShaderCorpus` 样本时**：再按需补 `daily-default`；它属于增强入口，不替代跨机器硬默认
4. **更完整的命令模板、定向 `test_casts` 复核、本地观察入口、参考批量入口与固定输出目录**，统一见 `08-当前代表集与Gate契约参考.md`（工作参考，当前主线推进**不必须读取**）

只要前两步已经稳定给出“代表 gate 无新增回归 + 默认行为证据为 `pass`”这组既有边界，本轮日常语义验证闭环就视为完成；默认不再为了覆盖率追加 `daily-default`、`test-data-batch` 或 L4。

### 可以作为第二阶段 gate

- `local-corpus-representatives`、`test_casts` 定向复核与 `test-data-batch` 参考批量都继续保留，但职责是补充证据或历史对照，而不是替代硬默认 gate
- 当前 behavior runner 依赖前置 roundtrip 固定产物，主要新增 `behavior-summary.json` 与 `behavior-artifacts/`；它不应被误解为重新生成整套 L1/L2 报告
- 对当前行为 harness，优先接受“`pass` / `warn` / `fail` / `deferred` 四态组合证据”，而不是强行要求所有候选一次性全绿；只有真正的执行错误才应视为 harness 本身未准备好
- 必要时使用 `./BuildScripts/build_and_install.sh` 进行运行时链路验证；默认保持脚本自带的 `user` 安装模式，不主动切到可能触发 `sudo` 的 `auto/system`

### 不应作为默认日常 gate

- 需要用户反复介入的 live 流程
- 高成本 GUI 操作
- 手动打开 Xcode 的视觉确认
- 工作区外分析
- 因缺少现成样本就要求用户协助准备环境

## 产物建议

### 推荐目录

```text
build/semantics-validation/
  roundtrip/
    test-data-representatives/
      behavior-summary.json
      behavior-summary.test-casts-verification.json
      behavior-artifacts/
    test-data-batch/
    local-corpus-representatives/
    daily-default/
    <timestamp>/                 # 非 preset / 临时试验运行
  behavior/<timestamp>/          # 仅在显式覆盖 output-root 时使用
  real-scene/<timestamp>/
```

### 报告建议

默认仍以结构化 JSON 报告为主；具体字段详见 `03-L1-IR-RoundTrip.md`、`04-L2-CanonicalCompareAndRiskGrading.md`、`05-L3-最小行为测试.md` 与 `06-L4-真实场景验证.md`。

当前控制面只要求按层稳定产出：

- L1/L2：`replay-summary.json`、`compile-summary.json`、`roundtrip-summary.json`、`compare-summary.json`、`risk-report.json`、`gate-summary.json`、`preset-manifest.json`
- L3：`behavior-summary.json`、按需的 `behavior-summary.*.json`
- L4：`live-summary.json`、`gputrace-summary.json`、`render-diff-summary.json`、`diagnostics-summary.json`

### 样本建议

- `test-data/`：最小模式覆盖，也是跨机器硬默认入口的主要来源
- `ShaderCorpus/`：真实样本代表集，但只作为本机已有条件下的增强证据
- 本目录后续若需新增专门样本选择策略，可另开子文档维护
- 首轮基线与历史细节下沉到 `07-首轮基线与历史进展归档.md`（历史参考，**不必须读取**）

## 当前结论

当前最优技术路线不是直接冲向 L4，而是：

1. **先持续守住 `SV-003` 的默认 gate 不回退**
2. **直接复用 `SV-006` 已落地的 `layeredDecision` 推进 `SV-004`**
3. **把 `SV-004` 收口到 `SV-004F` 这条最小默认执行面上，持续守住 `pass/pass` 边界、固定输出目录与 oracle ↔ `.ll` 同步护栏**
4. **只在 `SV-004F` 的纯离线证据仍不足时，最后才考虑 `SV-005`**

这条路线最符合当前仓库的工具现状，也最能保证 agent 自主推进时对用户依赖最低。
